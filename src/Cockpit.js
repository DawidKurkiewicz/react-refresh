import React from 'react';
const cockpit = (props) => {
    return (
        <div className="Cockpit">
            <h1>{props.title}</h1>
            <p>Welcome on my react app</p>
        </div>
    );
};

export default cockpit;